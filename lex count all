%{
#include <stdio.h>
int line=0,words=0,blanks=0,chars=0,i;
%}
%%
[#]blank++;
[\n]line++;
[^\t\n]+ {words++;chars+=yyleng;}
%%
int yywrap()
{
printf("char=%d,lines=%d,blanks=%d,words=%d",chars,line,blanks,words);
}
int main(int * argc,char *argv[]){
yyin=fopen(a[i].txt,"r");
yylex();
return 0;
}
